# TASK: this starter only defines DATABASE_URL. Add DIRECT_URL once you
# provision Render PostgreSQL, using the same database's non-pooled connection string.
DATABASE_URL=replace_with_render_internal_pooled_url
NODE_ENV=development
PORT=5000
